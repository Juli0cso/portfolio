/* Espelho do README.md do repositório Loja_ML, exibido no visualizador do
   portfólio. Gerado a partir do arquivo original — se o README mudar lá, este
   arquivo precisa ser regerado. */

export const clubeReadme = "# Clube da Economia\n\nPlataforma de curadoria de ofertas em produção. Um pipeline de automação coleta anúncios do Mercado Livre, uma API em Spring Boot valida e persiste os dados, e uma vitrine em React expõe apenas os descontos verificados contra o histórico real de preço.\n\n**No ar:** aplicação publicada em VPS própria, atrás de Nginx.\n**Canal:** [t.me/ClubDeconomia](https://t.me/ClubDeconomia)\n\n---\n\n## O problema\n\nSites de promoção sofrem de um vício conhecido: o \"desconto\" é calculado sobre um preço cheio que nunca existiu. O anunciante infla o valor de referência e o corte percentual vira ficção.\n\nEste projeto ataca isso guardando a série histórica de cada produto. O preço é reconsultado diariamente na API oficial e toda alteração fica registrada — o desconto exibido é aferível contra o que o item realmente custou.\n\n## Arquitetura\n\n```\n   n8n (coleta)                    Spring Boot API                  React SPA\n        │                                  │                            │\n        │  POST /api/webhook/produtos      │                            │\n        │  X-N8N-Token ────────────────▶   │                            │\n        │                                  │   GET /api/produtos/*      │\n        │  PATCH /api/produtos/afiliado    │   ◀────────────────────────┤\n        │  ────────────────────────────▶   │                            │\n        │                                  ▼\n        │                          PostgreSQL (Supabase)\n        │                                  │\n        │                     trigger: log_product_price_change\n        │                                  ▼\n        │                       product_price_history\n        │                                  ▲\n        └───────── @Scheduled ─────────────┘\n                 revalida preços na API do Mercado Livre\n```\n\nO n8n é a única porta de entrada de dados novos. A API não faz scraping: ela recebe, valida, autentica e persiste. O histórico não depende da aplicação lembrar de gravar — é uma trigger no banco, disparada por qualquer caminho que altere o preço.\n\n## Stack\n\n| Camada | Tecnologias |\n|---|---|\n| Backend | Java 17, Spring Boot 3.4, Spring Data JPA, Lombok |\n| Banco | PostgreSQL (Supabase), migrations versionadas, RLS |\n| Frontend | React, Vite, TailwindCSS |\n| Integração | n8n, API do Mercado Livre (OAuth client_credentials) |\n| Infra | Docker, Docker Compose, Nginx |\n| Testes | JUnit 5, Mockito, AssertJ |\n\n## Decisões técnicas\n\n**Cache de token OAuth.** O token do Mercado Livre é reaproveitado enquanto válido, com margem de 60 segundos antes do vencimento para não usar um token que expira em trânsito. Evita uma ida à rede por chamada.\n\n**Consulta em lote.** A rotina de preços fatia os produtos ativos em lotes de 20 — o limite documentado do endpoint multi-get — e acumula as alterações para uma única escrita com `saveAll`, em vez de um `save` por produto.\n\n**Histórico por trigger.** `log_product_price_change` roda no banco, não na aplicação. Garante que nenhuma alteração de preço escape do registro, independentemente do caminho que a provocou.\n\n**Escrita condicional.** Produtos cujo preço não mudou não são reescritos, o que evita atualizações inúteis e ruído no histórico.\n\n**Autenticação antes do processamento.** O webhook valida o `X-N8N-Token` antes de tocar no corpo da requisição, e separa erro do cliente (400) de falha do servidor (500).\n\n## API\n\nTodas as rotas sob `/api`.\n\n### Público\n\n| Método | Rota | Descrição |\n|---|---|---|\n| `GET` | `/produtos/ofertas` | Ofertas ativas, mais recentes primeiro |\n| `GET` | `/produtos/buscar` | Busca com filtros: `q`, `category`, `brand`, `minPrice`, `maxPrice` |\n| `GET` | `/produtos/{id}/historico` | Série histórica de preço do produto |\n| `GET` | `/produtos/categorias` | Categorias distintas em uso |\n| `GET` | `/categorias` | Catálogo de categorias |\n| `GET` | `/afiliados/gerar-link` | Geração de link de afiliado |\n\n### Integração — exige `X-N8N-Token`\n\n| Método | Rota | Descrição |\n|---|---|---|\n| `POST` | `/webhook/produtos` | Recebe uma oferta coletada pelo n8n |\n| `PATCH` | `/produtos/afiliado` | Associa o link de afiliado a um produto |\n| `PATCH` | `/produtos/{mlId}/telegram-sent` | Marca a oferta como publicada no canal |\n| `GET` | `/produtos/pendentes-afiliacao` | Fila de produtos sem link de afiliado |\n| `GET` | `/produtos/telegram-pendentes` | Fila de produtos ainda não publicados |\n\n## Rotinas agendadas\n\n| Horário | Rotina | Efeito |\n|---|---|---|\n| 03:30 | `deactivateStaleProducts` | Remove da vitrine produtos sem sincronização há 48h |\n| 04:00 | `updateProductPrices` | Revalida preços na API oficial, em lotes de 20 |\n\n## Como rodar\n\n**Requisitos:** Docker e Docker Compose. Para desenvolvimento do backend fora do container, Java 17 e Maven.\n\n```bash\ngit clone https://github.com/Juli0cso/Loja_ML.git\ncd Loja_ML\ncp .env.example .env\n```\n\nPreencha o `.env` com as credenciais do Supabase e do Mercado Livre, e então:\n\n```bash\ndocker compose up --build\n```\n\n| Serviço | Endereço |\n|---|---|\n| Vitrine | http://localhost |\n| API | http://localhost:8081 |\n\n### Variáveis de ambiente\n\n| Variável | Descrição |\n|---|---|\n| `SUPABASE_DB_URL` | JDBC do Postgres |\n| `SUPABASE_DB_USERNAME` | Usuário do banco |\n| `SUPABASE_DB_PASSWORD` | Senha do banco |\n| `ML_APP_ID` | App ID da aplicação no Mercado Livre |\n| `ML_CLIENT_SECRET` | Client secret da aplicação |\n| `N8N_SECRET` | Segredo compartilhado com o n8n |\n| `APP_CORS_ORIGINS` | Origens liberadas no CORS (opcional) |\n\nNenhuma credencial vive no código: todas entram por ambiente e são lidas via `@Value`. O `.env` não é versionado.\n\n## Testes\n\n```bash\ncd backend\nmvn test\n```\n\nA suíte cobre `MercadoLivreService` com JUnit 5 e Mockito, sem subir contexto Spring e sem tocar a rede — o `RestTemplate` é injetado como dublê. Os casos verificam o cache do token, a renovação após o vencimento, o descarte de itens que voltam com código diferente de 200, a conversão numérica e o envio dos ids numa única requisição.\n\n## Estrutura\n\n```\nbackend/\n  src/main/java/com/loja/vitrine/\n    controllers/   endpoints REST e DTOs\n    services/      integração com o Mercado Livre e regras do webhook\n    jobs/          rotinas agendadas\n    models/        entidades JPA\n    repositories/  Spring Data + consultas com filtro\n  src/test/java/   testes de unidade\nfrontend/          SPA React + Vite + Tailwind\nsupabase/\n  migrations/      evolução do schema, incluindo a trigger de histórico\ndocs/              workflow do n8n e contrato do webhook\ndocker-compose.yml\n```\n\n## Licença\n\nProjeto pessoal, sem licença de uso definida.\n"
